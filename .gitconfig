[push]
	default = current
[alias]
	s = status
	confg = config --global
	b = branch --color
    co = checkout
    df = diff
    log = log -p
    ci = commit -s 
    f = fetch
    p = push
    ps = push origin
    pl = pull origin
    a = add
    l = log
	d = diff
	po = push origin
	cp = cherry-pick
	dev = checkout development
	qa = checkout qa
	cm = checkout cm
	cd = checkout cd
	staging = checkout staging
	master = checkout master
	comp = log --oneline --left-right
[color]
	ui = true